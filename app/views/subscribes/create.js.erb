$('#flash').replaceWith("<%= j render partial: 'shared/flash_message' %>");

<% unless @subscribe.errors.any? %>
  $("#subscribe_btn").replaceWith("<%= j link_to "Unsubscribe", subscribe_path(@subscribe), id: "unsubscribe_btn", class: "btn btn-primary", remote: true, method: :delete %>")
<% end %>
